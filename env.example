# ============================================
# RepassIA API - Arquivo de Configuração
# ============================================
# Copie este arquivo para .env e preencha com seus valores
# cp env.example.txt .env

# ============================================
# SERVIDOR
# ============================================
# Ambiente de execução: development, production, test
NODE_ENV=development

# Porta onde a API será executada
PORT=8080

# URL base da API (usado para gerar URLs de imagens)
API_BASE_URL=http://localhost:8080

# ============================================
# BANCO DE DADOS (PostgreSQL)
# ============================================
# URL completa de conexão do Prisma
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://repassia:senha_segura@localhost:5432/repassia_db

# Configurações individuais (alternativa à DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=repassia
DB_PASSWORD=senha_segura
DB_NAME=repassia_db

# ============================================
# MINIO (Armazenamento de Arquivos)
# ============================================
# Endpoint do MinIO (sem http://)
MINIO_ENDPOINT=localhost

# Porta do MinIO
MINIO_PORT=9000

# Credenciais de acesso ao MinIO
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Nome do bucket onde as imagens serão armazenadas
MINIO_BUCKET=car-images

# Usar SSL/TLS (true/false)
MINIO_USE_SSL=false

# ============================================
# SMTP (Envio de Emails - Hostinger)
# ============================================
# Servidor SMTP
SMTP_HOST=smtp.hostinger.com

# Porta SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=587

# Usuário/email para autenticação SMTP
SMTP_USER=seu-email@seudominio.com

# Senha do email SMTP
SMTP_PASSWORD=sua_senha_smtp

# Email remetente (pode ser o mesmo que SMTP_USER)
SMTP_FROM=noreply@seudominio.com

# ============================================
# AUTENTICAÇÃO
# ============================================
# Chave secreta para assinar tokens JWT (use uma string aleatória forte)
JWT_SECRET=seu_jwt_secret_super_seguro_altere_em_producao

# Tempo de expiração do token JWT (ex: 24h, 7d, 30d)
JWT_EXPIRATION=24h

# Tempo de expiração do código de autenticação por email (em minutos)
AUTH_CODE_EXPIRATION=15

# Comprimento do código de autenticação (padrão: 6 dígitos)
AUTH_CODE_LENGTH=6

# ============================================
# CORS (Cross-Origin Resource Sharing)
# ============================================
# Origens permitidas (separadas por vírgula)
# Exemplo: http://localhost:3000,https://seusite.com,https://app.seusite.com
ALLOWED_ORIGINS=http://localhost:3000,https://seusite.com

# ============================================
# SCHEDULER (Limpeza Automática)
# ============================================
# Número de dias após os quais carros antigos serão removidos automaticamente
CAR_CLEANUP_DAYS=5

# Cron schedule para execução da limpeza
# Formato: minuto hora dia mês dia-da-semana
# Exemplo: 0 2 * * * = Diariamente às 2h da manhã
# Exemplo: 0 3 * * 0 = Toda segunda-feira às 3h
CLEANUP_SCHEDULE=0 2 * * *

# ============================================
# UPLOAD DE ARQUIVOS
# ============================================
# Tamanho máximo do arquivo em bytes (padrão: 5MB)
# 5242880 bytes = 5MB
MAX_FILE_SIZE=5242880

# Tipos de arquivo permitidos (separados por vírgula)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

